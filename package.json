{
    "name": "mileage-claims",
    "version": "1.0.13",
    "scripts": {
        "start": "npm run clean && parcel src/app.html --open",
        "clean": "rmdir /s /q dist || exit 0",
        "build": "npm run clean && parcel build src/app.html --no-source-maps"
    },
    "type": "module",
    "dependencies": {
        "@azure/msal-browser": "^4.26.0",
        "@picocss/pico": "^2.1.1",
        "ky": "^1.8.1",
        "redom-components": "^1.0.16"
    },
    "devDependencies": {
        "parcel": "^2.16.0",
        "redom": "^4.3.0"
    }
}