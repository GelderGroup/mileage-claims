{
    "name": "mileage-claims",
    "version": "1.0.13",
    "scripts": {
        "clean": "rimraf dist",
        "build": "npm run clean && parcel build src/index.html --no-source-maps && cpx \"staticwebapp.config.json\" dist",
        "start": "parcel src/index.html --port 1234 --open"
    },
    "type": "module",
    "dependencies": {
        "@picocss/pico": "^2.1.1",
        "ky": "^1.14.0",
        "redom-components": "^1.0.16"
    },
    "devDependencies": {
        "cpx2": "^8.0.0",
        "parcel": "^2.16.0",
        "redom": "^4.3.0",
        "rimraf": "^6.1.0"
    }
}
